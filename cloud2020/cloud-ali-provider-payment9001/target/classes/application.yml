server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 # Nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'  #暴露端口
